// jar包的名字
archivesBaseName = 'zookeeper-driver-client'

dependencies {
    compile(
            libraries.'zkCurator',
            libraries.'zookeeper',
            libraries.'commons-pool2',
            project(':zookeeper-driver-helper')
    )
}

