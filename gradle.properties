#all the property is example and u can override it by -P in command line
#we should not define the DB and maven repository's username password here,
#should override them with -P in command line

# define the project attribute
group=org.sagesource
version=1.0-SNAPSHOT
name=zookeeper-driver

# define the gradle's version
gradleVersion=3.2.1
gradle.templates.dir=gradle/gradle-templates
gradle.templates.ver=1.4

#Maven repositories
springMavenRepositories=https://repo.spring.io/libs-milestone
aliyunMavenRepositories=http://maven.aliyun.com/nexus/content/groups/public

#MyBatis Generator Config
modelPackage=org.sagesource.entity
#生成的mapper接口类所在包
mapperPackage=org.sagesource.mapper
#生成的mapper xml文件所在包，默认存储在resources目录下
sqlMapperPackage=mybatis_mapper

